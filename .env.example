# Animation Reviewer Environment Configuration

# API URL - Leave empty for localStorage-only mode (local development)
# Set to your API Gateway URL for cloud sync
VITE_API_URL=

# User ID for annotations - defaults to "default" if not set
# Change this if you want to separate annotations by user
VITE_USER_ID=default
